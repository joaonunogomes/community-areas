## What changed?

<!-- Briefly describe what you added or modified. -->

## Area(s) affected

<!-- List the area slug(s) this PR touches, e.g. `sameiro`, `penedo-da-gavea`. -->

-

## Type of change

- [ ] New area
- [ ] Update to existing area (data, images, metadata)
- [ ] Other (describe below)

## Checklist

- [ ] `data.json` follows the documented schema
- [ ] `index.json` entry matches `data.json` (`name`, `country`, `city`, `type`, `boulderCount`)
- [ ] `version` in `index.json` is set to `1` (new area) or bumped by 1 (update)
- [ ] All images are in `.jpg`/`.jpeg` format
- [ ] All images are under 300KB
